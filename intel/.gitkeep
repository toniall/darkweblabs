# This folder holds the visual intelligence suite generated by `./lab report`.
# It is bind-mounted read-only into the workstation at /intel (see compose.yml).
# The generated *.html pages are not committed; run ./lab report to build them.
